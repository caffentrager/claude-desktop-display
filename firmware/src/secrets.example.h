#pragma once

// Copy this file to secrets.h and fill in your own values.
// secrets.h is gitignored so real credentials never get committed.

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"

// Run `claude setup-token` (Claude Code CLI) on any machine you're logged
// into Claude on, and paste the token it prints here. This is a personal
// credential - treat it like a password.
#define CLAUDE_OAUTH_TOKEN "PASTE_YOUR_OAUTH_TOKEN_HERE"
